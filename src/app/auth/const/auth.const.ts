const loginFlagKey = 'loggedIn';
const userTokenKey = 'userToken';
const authorizationHeader = 'Authorization';
const tokenPrefix = 'Bearer';

export const authConstants = { loginFlagKey, userTokenKey, authorizationHeader, tokenPrefix };
