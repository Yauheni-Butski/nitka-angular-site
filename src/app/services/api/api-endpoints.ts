const baseAPIurl = 'http://work.local/api'; // 'http://localhost:13021/api';

const categoriesUrl = `${baseAPIurl}/categories`;
const sectionUrl = `${baseAPIurl}/section`;
const loginUrl = `${baseAPIurl}/login`;
const contactUrl = `${baseAPIurl}/contact`;
const adminDashboardUrl = `${baseAPIurl}/admin/dashboard`;

export const apiEndpoints = { categoriesUrl, sectionUrl, loginUrl, contactUrl, adminDashboardUrl };
