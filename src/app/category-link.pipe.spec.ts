import { ImageCardLinkPipe } from './shared/pipes/image-card-link.pipe';

describe('ImageCardLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageCardLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
