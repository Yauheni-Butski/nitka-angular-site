import { ForTop6Directive } from './shared/directives/for-top6.directive';

describe('ForTop6Directive', () => {
  it('should create an instance', () => {
    const directive = new ForTop6Directive();
    expect(directive).toBeTruthy();
  });
});
